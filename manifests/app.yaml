apiVersion: app/v1
kind: deployment
metadata:
 name: myapp-v1
spec:

 selector:
  matchLabels:
  app: myapp
 replicas: 1 
   template:
    metadata:
      labels:
        app: myapp
 spec:
      containers:
      - image: nanajanashia/argocd-app:1.0
        name: myapp
        port:
               